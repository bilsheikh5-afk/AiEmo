services:
  - type: web
    name: mindsync-backend
    runtime: node
    plan: free
    rootDirectory: ./backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
